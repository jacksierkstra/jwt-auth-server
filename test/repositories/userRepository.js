var should = require('should');
var assert = require('assert');
var request = require('supertest');

describe('User CRUD Actions', function() {

  it('Should return an error message when a user has already been registered.',
  function(done) {
    done();
  });

});
